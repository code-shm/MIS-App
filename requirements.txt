# Core data stack
pandas>=2.1
numpy>=1.26
pyarrow>=14.0          # Parquet I/O for BigQuery-friendly columnar storage

# NLP / sentiment
vaderSentiment>=3.3.2  # Lexicon+rule sentiment on complaint narratives

# Machine learning
scikit-learn>=1.4      # Escalation classifier + churn-risk model

# Visualisation (report figures)
matplotlib>=3.8
seaborn>=0.13

# Ingestion
requests>=2.31
tqdm>=4.66

# BigQuery (optional — only needed for the cloud upload step)
google-cloud-bigquery>=3.17
google-cloud-bigquery-storage>=2.24
db-dtypes>=1.2
